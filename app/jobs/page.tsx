import Link from "next/link";
import { branchLabels, militaryJobs } from "@/data/jobs";
export default function JobsPage() { return <main className="min-h-screen bg-stone-50 px-5 py-8 text-stone-950"><div className="mx-auto max-w-5xl"><Link className="text-lg font-bold" href="/">특급꿀벌</Link><h1 className="mt-10 text-3xl font-bold">직무 탐색</h1><p className="mt-3 text-stone-600">샘플 직무군을 가볍게 둘러보고 자세한 설명을 확인해 보세요.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{militaryJobs.map((job) => <article className="rounded-2xl border border-stone-200 bg-white p-5" key={job.id}><p className="text-sm font-semibold text-emerald-700">{branchLabels[job.branch]} · {job.category}</p><h2 className="mt-2 text-lg font-bold">{job.name}</h2><p className="mt-3 text-sm leading-6 text-stone-600">{job.shortDescription}</p><Link className="mt-5 inline-flex text-sm font-semibold text-emerald-700 hover:underline" href={"/jobs/" + job.slug}>자세히 보기 →</Link></article>)}</div></div></main>; }
